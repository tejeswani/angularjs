ng serve -> Run your application. Code changes will be reflected on the fly.
ng generate component <component-name> -- 
ng generate directive <directive-name> -- |pipe|service|class|guard|interface|enum|module
ng generate interface <interface-name> --
ng generate enum <enum-name> -- Enum are very useful in all the programming languages. What is Enum ?
ng build
ng test -- what is Karma ? Karma is a testing framework for Angular. This is very important. Karma is a test runner that fits all our needs.
ng e2e -- end2end test
ng help
npm install -g @angular/cli@17
ng config -- to edit angular.json file -- Note: Option names in the configuration file must use "camelCase", while option names supplied to commands must be dash-case.


npm i -g npm-check-updates : added 1 package in 5s
ncu -u : Upgrading C:\Users\Public\Tejeswani\angularjs\package.json (npm-check-updates - ncu)
npm i : npm install

ng --version: 18.2.5
Fixing the TypeScript version issue : Failed to initialize Angular compilation - The Angular Compiler requires TypeScript >=5.4.0 and <5.6.0 but 5.6.2 was found instead.
npm uninstall -d TypeScript
npm install -d typescript@5.6.0

